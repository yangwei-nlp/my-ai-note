# 指令微调 (Instruction Tuning)

## 简介

指令微调是一种让大语言模型学会理解和执行各种指令的训练方法。通过在多样化的指令数据集上训练，模型能够泛化到未见过的任务，实现"零样本"和"少样本"学习能力。

## 核心思想

```
预训练模型
    ↓ instruction tuning
"通用任务执行器"
```

### 为什么需要指令微调？

| 能力 | 预训练模型 | 指令微调后 |
|------|-----------|-----------|
| 遵循格式 | 弱 | 强 |
| 多任务泛化 | 无 | 有 |
| 零样本能力 | 无 | 有 |
| 对话能力 | 差 | 好 |

## 数据集

### 开源指令数据集

| 数据集 | 规模 | 特点 |
|--------|------|------|
| **Alpaca** | 52K | Self-Instruct 生成的指令 |
| **Dolly** | 15K | 人工编写的多样化任务 |
| **FLAN** | 数千任务 | 基于 NLP 基准任务转换 |
| **ShareGPT** | 大规模 | 真实用户对话数据 |
| **LIMA** | 1K | 高质量少即是多 |
| **UltraChat** | 1.5M | 两个 GPT-3.5 对话生成 |

### 数据格式

#### Alpaca 格式

```json
{
  "instruction": "将以下句子翻译成法语",
  "input": "The cat is sleeping on the sofa.",
  "output": "Le chat dort sur le canapé."
}
```

#### ShareGPT 格式

```json
{
  "conversations": [
    {"from": "human", "value": "写一首关于春天的诗"},
    {"from": "gpt", "value": "春风拂面花自开，燕子归来筑巢台..."},
    {"from": "human", "value": "再写一首关于秋天的"},
    {"from": "gpt", "value": "秋风瑟瑟叶飘零，夕阳西下映山青..."}
  ]
}
```

### 任务类型

- **分类**：情感分析、主题分类
- **抽取**：NER、关系抽取
- **问答**：开放域问答、阅读理解
- **生成**：摘要、翻译、创作
- **推理**：逻辑推理、数学计算
- **代码**：代码生成、代码解释

## 训练策略

### 1. 混合训练

将多种任务类型的数据混合训练：

```python
# 数据混合比例
data_mixture = {
    "alpaca": 0.3,
    "dolly": 0.2,
    "code": 0.2,
    "math": 0.1,
    "conversation": 0.2,
}
```

### 2. 任务模板化

使用统一的指令模板提高泛化能力：

```
### 指令：
{instruction}

### 输入：
{input}

### 输出：
{output}
```

### 3. 多轮对话训练

支持对话上下文的训练：

```python
# 构建对话历史
def build_conversation_prompt(conversations):
    prompt = ""
    for turn in conversations:
        if turn["from"] == "human":
            prompt += f"User: {turn['value']}\\n"
        else:
            prompt += f"Assistant: {turn['value']}\\n"
    return prompt
```

## 关键技术

### 1. Self-Instruct

用模型自身生成指令数据：

```
步骤1：准备种子人工指令（175条）
步骤2：让模型生成新指令和输入
步骤3：让模型生成输出
步骤4：过滤低质量数据
步骤5：重复直到数据规模足够
```

### 2. 指令多样性

提高数据多样性的方法：

- **改写指令**：用不同措辞表达相同任务
- **组合任务**：多步骤复杂指令
- **对抗性示例**：困难的边界情况

### 3. 数据质量筛选

```python
# 基于奖励模型或规则过滤
def filter_quality(data):
    # 1. 长度过滤
    if len(data["output"]) < 10:
        return False
    
    # 2. 重复过滤
    if is_duplicate(data):
        return False
    
    # 3. 质量打分
    if quality_score(data) < threshold:
        return False
    
    return True
```

## 评估方法

### 自动评估

| 指标 | 适用场景 |
|------|----------|
| **Exact Match** | 分类、抽取任务 |
| **F1 Score** | NER、信息抽取 |
| **BLEU/ROUGE** | 翻译、摘要 |
| **Code Execution** | 代码生成 |

### 人工评估

- **有用性 (Helpfulness)**：回答是否解决了问题
- **真实性 (Honesty)**：回答是否准确
- **无害性 (Harmlessness)**：回答是否有害

## 参考资料

- [Alpaca: A Strong, Replicable Instruction-Following Model](https://crfm.stanford.edu/2023/03/13/alpaca.html)
- [Training language models to follow instructions with human feedback](https://arxiv.org/abs/2203.02155)
- [The Flan Collection](https://arxiv.org/abs/2301.13688)
- [LIMA: Less Is More for Alignment](https://arxiv.org/abs/2305.11206)
