# 多模态微调 (Multimodal Fine-Tuning)

## 简介

多模态微调是指对能够处理两种或多种模态（如文本、图像、音频、视频）的模型进行微调。当前最主流的是视觉-语言模型（Vision-Language Models）的微调。

## 主流模型架构

### 1. Encoder-Decoder 架构

```
图像编码器 → 多模态融合 → 文本解码器
   ↓              ↓           ↓
 ViT/CLIP     Cross-Attn    LLM
```

**代表模型**：LLaVA、MiniGPT-4、BLIP-2

### 2. 投影层架构

```
图像 → 图像编码器 → 投影层 → 输入到大语言模型
                          ↓
                    与文本 token 拼接
```

**代表模型**：LLaVA 系列

## 常见模型

| 模型 | 视觉编码器 | 语言模型 | 特点 |
|------|-----------|----------|------|
| **LLaVA** | CLIP ViT | Vicuna/LLaMA | 开源、灵活 |
| **MiniGPT-4** | ViT + Q-Former | Vicuna | 视觉特征丰富 |
| **BLIP-2** | ViT | OPT/Flan-T5 | 冻结视觉编码器 |
| **Qwen-VL** | ViT | Qwen | 中文支持好 |
| **Yi-VL** | ViT | Yi | 中英双语 |

## 微调方式

### 1. 全量微调

需要解冻视觉编码器、投影层和语言模型，显存需求大。

### 2. 冻结视觉编码器

只微调投影层和/或语言模型：

```python
# 冻结视觉编码器
for param in model.vision_tower.parameters():
    param.requires_grad = False

# 只训练投影层和 LLM
for param in model.mm_projector.parameters():
    param.requires_grad = True
for param in model.language_model.parameters():
    param.requires_grad = True
```

### 3. LoRA 微调

```python
from peft import LoraConfig, get_peft_model

lora_config = LoraConfig(
    r=128,
    lora_alpha=256,
    target_modules=["q_proj", "v_proj", "k_proj", "o_proj", "gate_proj", "up_proj", "down_proj"],
    lora_dropout=0.05,
    bias="none",
    task_type="CAUSAL_LM",
)

model.language_model = get_peft_model(model.language_model, lora_config)
```

## 数据格式

### LLaVA 格式

```json
{
  "id": "000000000001",
  "image": "image_path.jpg",
  "conversations": [
    {
      "from": "human",
      "value": "<image>\\n描述这张图片的内容"
    },
    {
      "from": "gpt",
      "value": "这张图片展示了一个阳光明媚的海滩场景..."
    }
  ]
}
```

### 数据类型

| 任务类型 | 示例 |
|----------|------|
| **图像描述** | "描述这张图片" |
| **视觉问答** | "图中有几只猫？" |
| **图文检索** | "找一张有狗的图片" |
| **目标定位** | "请框出图中的苹果" |
| **多轮对话** | 连续的图文交互 |
| **OCR** | "读取图中的文字" |

## 训练阶段

### 阶段1：特征对齐预训练

**目标**：让语言模型学习理解视觉特征

```python
# 使用大规模图文对数据 (如 CC3M、SBU、LAION)
# 冻结视觉编码器和语言模型
# 只训练投影层

training_args_pretrain = TrainingArguments(
    num_train_epochs=1,
    learning_rate=1e-3,  # 投影层使用较大学习率
    ...
)
```

### 阶段2：视觉指令微调

**目标**：让模型学会遵循多模态指令

```python
# 使用视觉指令数据 (如 LLaVA-Instruct)
# 冻结视觉编码器
# 训练投影层和语言模型

training_args_finetune = TrainingArguments(
    num_train_epochs=1,
    learning_rate=2e-5,
    ...
)
```

## 幻觉问题

多模态模型容易"幻觉"（生成与图片不符的内容）：

### 缓解策略

1. **数据层面**：高质量数据、负样本训练
2. **模型层面**：Grounding 机制、参考模型约束
3. **训练层面**：RLHF、DPO 对齐

## 应用场景

- **图文对话**：多轮图文交互
- **视觉搜索**：基于图片的检索
- **辅助视觉**：帮助视障人士
- **教育**：图像问答、作业辅导
- **电商**：商品理解、推荐

## 参考资料

- [LLaVA: Large Language and Vision Assistant](https://llava-vl.github.io/)
- [BLIP-2: Bootstrapping Language-Image Pre-training](https://arxiv.org/abs/2301.12597)
- [MiniGPT-4: Enhancing Vision-Language Understanding](https://minigpt-4.github.io/)
- [Multimodal Reward Modeling](https://arxiv.org/abs/2406.16338)
